require 'md_inc/version'
require 'md_inc/md_inc_commands'

module MdInc
  class TextProcessor
    def process_stream(s)
      Commands.process(s.read)
    end

    def process_file(path)
      Commands.process(File.read(path))
    end
  end
end
